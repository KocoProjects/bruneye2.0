name: bruneye
description: "Brunel seeing art"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter_vision:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  flutter_svg:
  camera_android:
  camera:
  path: ^1.8.3
  path_provider: ^2.1.2
  tflite_flutter: ^0.11.0
  flutter_tts:
  avatar_glow:
  shared_preferences:
  http:
  translator:


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/eye-studying-logo.svg
    - assets/labels.txt
    - assets/best_float32.tflite